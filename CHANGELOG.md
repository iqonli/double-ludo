## v0.42.2

- 继续强化游戏页、联机开房页和管理页的窄容器及手机布局。
- 游戏内弹窗保留安全边距和圆角，长内容在弹窗内部滚动，操作按钮在窄屏纵向排列。
- 联机开房页和管理页删除操作改用响应式自定义确认弹窗，不再调用浏览器原生确认框。
- 所有用户可见的服务器联机登录码示例统一为`12345A`。
- 同一IP的错误登录码尝试间隔改为1秒。
- 同一IP最多拥有5个账号，并且同一时刻只能管理1个账号。
- 新IP接管账号后，账号房间归属同步转移；登录第6个账号时自动删除最早拥有的账号及其房间。
- 取消IP房间数量限制，每个账号仍最多运行5个房间。
- 联机开房页重做标题、图标、账号入口、注意事项和复制成功气泡。
- 玩家开房页新增对局导入与导出，并显示完整IP。
- 管理页改为独立资源，根据在线或局域网模式切换语料，新增账号管理和完整IP显示。
- 项目版本推进到0.42.2。

## v0.42.1 Render适配热修（服务名dlol）

- Render Blueprint服务名固定为`dlol`，默认公开地址为`https://dlol.onrender.com`。
- 游戏、账号开房和管理API加入分阶段404重试：100ms×10、200ms×10、300ms×5。
- 旧兼容轮询间隔从500ms调整为1000ms，25秒长轮询保持不变。
- 在线模式未配置`ADMIN_PASSWORD`时，`/admin`显示明确配置说明，不再返回伪装404。

# 更新记录

## v0.42.1

- 清理旧版浏览器冒烟测试、历史专项脚本、重复测试报告和未使用的开发文件。
- “返回原局”移动到准备页“关于”按钮左侧。
- 准备页移除副标题，并在“双飞”左侧内嵌76×76项目图标。
- 游戏顶栏在标题左侧内嵌32×32项目图标。
- 游戏页、仓库入口页和服务端管理页统一使用蓝色棋子标签页图标。
- 项目版本推进到0.42.1。

## v0.41

- 联机三6遣返改为双方确认：受罚方可申请反悔或接受，对方可拒绝或同意。
- 拒绝后受罚方仍可再次申请，次数不限；同意后由服务端执行三6遣返反悔并广播恢复动画。
- 对方未收到申请时不再显示受罚方的三6弹窗。
- game.html内所有局域网聊天卡片在没有已保存尺寸时默认高度为500px。
- 联机直接发送`undo-defeat`命令会被服务端拒绝，反悔必须经过对方审批。

# v0.40

- 四枚同色棋子全部完成时，数字 `4` 改为显示在四枚棋子的几何中心。
- 完成数量标签在棋盘旋转时自动反向补偿，始终保持观看正向。
- 两类联机灵动光团尺寸改为150–200px，持续时间改为500–1000ms。
- 游戏端与服务端局域网聊天背景闪烁颜色改为 `#faefc0`。
- 游戏左右栏默认宽度统一为350px。
- 极速双飞联机开局改为A/B各自一次性投掷，投掷由服务端生成并同步。
- 玩家只能点击自己的投掷按钮，自己的按钮使用淡绿色背景。
- 玩家B完成自己的投掷前不能准备。
- 只有玩家A从“双飞”重新切换到“极速双飞”时，双方投掷机会才重置。
- 总点数相同时由服务端自动比较骰面；仍相同则随机决定先手。
- 大厅存档格式升级到版本4，新增 `lobbySpeedRolls`。
- 修复玩家A收到玩家B投掷结果后，本地先手状态未同步导致无法开始极速双飞的问题。
